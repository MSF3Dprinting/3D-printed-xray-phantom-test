numpy>=1.26
scipy>=1.11
scikit-image>=0.22
pydicom>=2.4
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.0
pillow>=10.0
matplotlib>=3.8
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
pytest>=8.0
# fastapi.testclient needs whichever HTTP client its Starlette expects, and that
# changed: Starlette < 1.0 imports httpx, Starlette >= 1.0 imports httpx2 and
# fails at IMPORT time without it — which looks like a broken test suite rather
# than a missing dependency. Installing both is harmless and covers either.
httpx>=0.27
httpx2>=2.12; python_version >= "3.9"

# --- server deployment only (not needed to run locally via run_app.py)
# gunicorn>=22.0
# uvicorn-worker>=0.2   # replaces the deprecated uvicorn.workers.UvicornWorker
